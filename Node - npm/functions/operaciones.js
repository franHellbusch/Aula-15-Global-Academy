export const sumar = (n1, n2) => {
  return n1 + n2;
};

// export (exportar puntualmente una function, variable, class, etc, forma mas actual)
export const restar = (n1, n2) => {
  return n1 - n2;
};

const multiplicar = (n1, n2) => {
  return n1 * n2;
};

export default multiplicar;

// module.exporst (forma mas antigua de exportar informacion de los archivos js )
// module.exports = {
//   sumar,
//   restar,
// };
